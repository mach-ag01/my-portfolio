# Augustine Mbugua — Premium Portfolio

## Included
- Responsive premium portfolio
- Your profile photo in `assets/profile.png`
- Dark/light mode
- GitHub and LinkedIn buttons (replace placeholders in `script.js`)
- Downloadable CV
- Project visual screenshots in `assets/projects/`
- Contact form using FormSubmit
- Vercel-ready static site

## Important before deployment

### 1. GitHub
GitHub is already configured as:
`https://github.com/mach-ag01`

### 2. Contact form
The form is configured for `mach.ag026@gmail.com` through FormSubmit. On first use, FormSubmit may send an activation/confirmation email.

Replace the `_next` value in `index.html` with your actual Vercel URL after deployment:
`https://YOUR-VERCEL-DOMAIN.vercel.app/#contact`

### 3. Deploy
Upload the complete folder to GitHub, then import the repository into Vercel. No build command is required for this static site.
